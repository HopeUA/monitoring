import React from 'react';
import Styles from './styles.scss';

/* eslint-disable react/prefer-stateless-function */
const NotFound = () => (
    <section className = { Styles.notFoundComponent }>
        <div>Not found</div>
    </section>
);

export default NotFound;
